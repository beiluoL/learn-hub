---
title: CRUD 映射
category: java
module: java-mybatis
subcat: roadmap
timeline: false
level: medium
tier: core
readMinutes: 12
tags: MyBatis
summary: select/insert/update/delete 与参数。
order: 2
---

- `parameterType` / `resultType` / `resultMap`。
- `useGeneratedKeys` 取自增主键。
- 多参数用 `@Param` 或 Map。
- `<selectKey>` 兼容不同数据库主键。
- 返回 Map 或 DTO 灵活映射。

**自查清单**
- [ ] 会写 CRUD
- [ ] 会取主键
- [ ] 会用 @Param
